class Neighborhood():
    def __init__(self, neighbor_list, is_alone, synchronized_neighbors, current_neighbors):
        self.neighbor_list = neighbor_list
        self.is_alone = is_alone
        self.synchronized_neighbors = synchronized_neighbors
        self.current_neighbors = current_neighbors
